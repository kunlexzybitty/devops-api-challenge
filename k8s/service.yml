apiVersion: v1
kind: Service
metadata:
    name: devopsapichallenge
spec:
    type: LoadBalancer
    ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
    selector:
        app: devopsapichallenge